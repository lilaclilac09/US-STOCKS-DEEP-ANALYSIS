name: data-refresh-and-deploy

on:
  schedule:
    - cron: '0 12 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Refresh market data
        run: node scripts/data-refresh.mjs
        env:
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}

      - name: Validate data
        run: npm run validate:data

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install requests beautifulsoup4

      - name: Fetch equity fundamentals
        run: python3 scripts/fetch-fundamentals.py
        continue-on-error: true

      - name: Fetch cislunar mission data
        run: python3 scripts/fetch-cislunar.py
        continue-on-error: true

      - name: Generate Markdown files
        run: python3 scripts/generate-markdown.py
        continue-on-error: true

      - name: Commit refreshed data and generated Markdown
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add data/*.json content/**/*.md
          git diff --cached --quiet || git commit -m "ops: automated data refresh with generated content ($(date +%Y-%m-%d))"
          git push

  deploy:
    needs: refresh
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build:prod
        env:


      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
